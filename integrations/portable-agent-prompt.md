# Portable Agent Prompt

Read this repository's `SKILL.md` and compile the URL I provide into an evidence-backed design-language package.

Use either the `site2style` CLI or the repository's MCP tools. Do not assume a vendor-specific browser plugin is installed. Run the browser doctor, capture the `full` responsive profile, generate `style-profile.json`, synthesize `STYLE.md` from measured evidence, and render `style-board.html`. Separate observations, measurements, transferable rules, confidence, and retain/reinterpret/replace boundaries. Before delivery, run `site2style bundle` and return one `design-package/` with `START-HERE.md`, `AGENT-HANDOFF.md`, and `manifest.json`, so another Session can reuse it without this conversation. Stop after the style package unless I request implementation. Report evidence gaps and never treat source branding, copy, photography, icons, or campaign concepts as automatically reusable style.
